name: Auto Merge

on:
  push:
    branches:
      - main

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Merge changes
        uses: pullreminders/merge-me-action@v1
